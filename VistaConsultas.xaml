<ui:FluentWindow x:Class="Proyecto_GRRLN_expediente.VistaConsultas"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:local="clr-namespace:Proyecto_GRRLN_expediente"
                 Title="Búsqueda Avanzada" Height="800" Width="1200"
                 WindowStartupLocation="CenterScreen"
                 WindowState="Maximized"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 Background="Transparent"
                 Loaded="UserControl_Loaded">

    <Window.Resources>
        <local:SemaforoColorConverter x:Key="SemaforoConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" Margin="30,10,30,30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                <ui:Button Content="Cerrar Búsqueda" Icon="Dismiss24" Appearance="Secondary" 
                           Height="36" Cursor="Hand" Margin="0,0,15,0" 
                           Click="BtnRegresar_Click"/>
                <TextBlock Text="BÚSQUEDA AVANZADA DE ASUNTOS" FontSize="22" FontWeight="Black" Foreground="#2C3E50" VerticalAlignment="Center"/>
            </StackPanel>

            <Border Grid.Row="1" Background="White" CornerRadius="8" BorderBrush="#EAEDED" BorderThickness="1" Padding="25" Margin="0,0,0,25">
                <Border.Effect>
                    <DropShadowEffect Color="#DDDDDD" BlurRadius="10" ShadowDepth="2" Opacity="0.4"/>
                </Border.Effect>

                <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <StackPanel Margin="0,0,25,10">
                        <TextBlock Text="Palabra clave / Oficio:" FontWeight="SemiBold" Foreground="#555555" Margin="0,0,0,8"/>
                        <ui:TextBox x:Name="TxtBusqueda" Width="260" Height="38" CharacterCasing="Upper" ClearButtonEnabled="True" PlaceholderText="Ej: Folio o Nombre"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,25,10">
                        <TextBlock Text="Estado del Trámite:" FontWeight="SemiBold" Foreground="#555555" Margin="0,0,0,8"/>
                        <ComboBox x:Name="CmbEstado" Width="220" Height="38" FontSize="14" VerticalContentAlignment="Center" DisplayMemberPath="Descripcion" SelectedValuePath="Id"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,25,10">
                        <TextBlock Text="Sede SAP:" FontWeight="SemiBold" Foreground="#555555" Margin="0,0,0,8"/>
                        <ComboBox x:Name="CmbSede" Width="220" Height="38" FontSize="14" VerticalContentAlignment="Center" DisplayMemberPath="Descripcion" SelectedValuePath="Id"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,22,0,10" VerticalAlignment="Center">
                        <Button Width="120" Height="38" Margin="0,0,10,0" Cursor="Hand" Click="BtnBuscar_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="FondoBoton" Background="#00843D" CornerRadius="4">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <ui:SymbolIcon Symbol="Search24" Foreground="White" Margin="0,0,8,0" FontSize="18"/>
                                            <TextBlock Text="Buscar" Foreground="White" FontWeight="Bold" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="FondoBoton" Property="Background" Value="#00A84D"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="FondoBoton" Property="Background" Value="#005C2B"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <ui:Button Content="Limpiar" Click="BtnLimpiar_Click" Width="110" Height="38" 
                                   Appearance="Secondary" Cursor="Hand" Icon="ArrowCounterclockwise24"/>
                    </StackPanel>
                </WrapPanel>
            </Border>

            <Border Grid.Row="2" Background="Transparent" CornerRadius="8">
                <ListView x:Name="lvResultados" 
                          Background="Transparent" 
                          BorderThickness="0"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          MouseDoubleClick="lvResultados_MouseDoubleClick">

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Margin" Value="0,0,0,12"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="CardBorder" Background="White" CornerRadius="8" BorderThickness="1" BorderBrush="#EAECEE" ClipToBounds="True">
                                <Border.RenderTransform>
                                    <TranslateTransform Y="0"/>
                                </Border.RenderTransform>

                                <Border.Effect>
                                    <DropShadowEffect x:Name="CardShadow" Color="#B2BABB" BlurRadius="6" ShadowDepth="1" Opacity="0.3"/>
                                </Border.Effect>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="6"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Background="{Binding Converter={StaticResource SemaforoConverter}}"/>

                                    <Grid Grid.Column="1" Margin="20,15,20,15">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="100"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                            <TextBlock Text="FOLIO" FontSize="11" Foreground="#A6ACAF" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding Id}" FontSize="20" FontWeight="Black" Foreground="#212F3D" Margin="0,2,0,0"/>
                                        </StackPanel>

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,20,0">
                                            <WrapPanel Orientation="Horizontal" Margin="0,0,0,6">
                                                <TextBlock Text="{Binding TituloAsunto}" FontSize="16" FontWeight="Bold" Foreground="#283747" VerticalAlignment="Bottom"/>
                                                <TextBlock Text=" • " FontSize="14" Foreground="#BDC3C7" Margin="8,0" VerticalAlignment="Bottom"/>
                                                <TextBlock Text="{Binding CategoriaCorta}" FontSize="12" Foreground="#7F8C8D" FontWeight="Medium" VerticalAlignment="Bottom" FontStyle="Italic"/>
                                            </WrapPanel>

                                            <StackPanel Orientation="Horizontal">
                                                <ui:SymbolIcon Symbol="Building24" FontSize="15" Foreground="#7F8C8D" Margin="0,0,6,0"/>
                                                <TextBlock Text="{Binding SedeNombre}" FontSize="13" Foreground="#566573" FontWeight="Medium"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center">
                                            <Border Background="#EBF5FB" CornerRadius="12" Padding="12,6" Margin="0,0,0,10" HorizontalAlignment="Right">
                                                <StackPanel Orientation="Horizontal">
                                                    <Ellipse Width="8" Height="8" Fill="#2980B9" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                                    <TextBlock Text="{Binding EstatusNombre}" FontSize="12" FontWeight="Bold" Foreground="#2980B9"/>
                                                </StackPanel>
                                            </Border>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                                <ui:SymbolIcon Symbol="Clock24" FontSize="15" Foreground="#95A5A6" Margin="0,0,6,0"/>
                                                <TextBlock Text="{Binding FechaLimite}" FontSize="13" Foreground="#7F8C8D" FontWeight="SemiBold"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                </Grid>

                                <Border.Triggers>
                                    <EventTrigger RoutedEvent="MouseEnter">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="CardBorder" 
                                                                 Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" 
                                                                 To="-3" Duration="0:0:0.2" />

                                                <DoubleAnimation Storyboard.TargetName="CardShadow" 
                                                                 Storyboard.TargetProperty="BlurRadius" 
                                                                 To="15" Duration="0:0:0.2" />

                                                <DoubleAnimation Storyboard.TargetName="CardShadow" 
                                                                 Storyboard.TargetProperty="ShadowDepth" 
                                                                 To="4" Duration="0:0:0.2" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>

                                    <EventTrigger RoutedEvent="MouseLeave">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="CardBorder" 
                                                                 Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" 
                                                                 To="0" Duration="0:0:0.2" />

                                                <DoubleAnimation Storyboard.TargetName="CardShadow" 
                                                                 Storyboard.TargetProperty="BlurRadius" 
                                                                 To="6" Duration="0:0:0.2" />

                                                <DoubleAnimation Storyboard.TargetName="CardShadow" 
                                                                 Storyboard.TargetProperty="ShadowDepth" 
                                                                 To="1" Duration="0:0:0.2" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Border.Triggers>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>
        </Grid>
    </Grid>
</ui:FluentWindow>